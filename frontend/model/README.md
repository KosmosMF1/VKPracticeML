# Сюда кладём экспортированную модель

Скопируйте сюда содержимое `tfjs_model/`, полученное после запуска
`export.sh` (файл `model.json` + `.bin`-шарды весов). Подробности — в
`../README.md`, раздел "Подключение модели".
